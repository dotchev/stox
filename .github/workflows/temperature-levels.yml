name: Update Temperature Levels

on:
  schedule:
    # Every Saturday at 06:37 UTC. Off the top of the hour on purpose:
    # GitHub deprioritizes scheduled runs under load, and :00 is the most
    # congested slot -- earlier runs at 06:00 landed 4-6 hours late.
    - cron: '37 6 * * 6'
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Update temperature levels
        run: python temperature-levels.py

      - name: Commit and push if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --quiet || git commit -m "Update temperature levels"
          git push
